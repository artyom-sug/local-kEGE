window.KT_TASK_API = {
    async loadTaskMeta(taskId) {
      // TODO:
      // Здесь будет код запроса к открытому API бэкенда kompege.ru.
      // Нужно будет выяснить:
      // 1) базовый URL API;
      // 2) как по taskId получить тип задачи;
      // 3) как получить сложность;
      // 4) нужна ли авторизация / есть ли CORS-ограничения.
      //
      // Ожидаемый результат:
      // return {
      //   type: "...",
      //   difficulty: "..."
      // };
  
      return {
        type: null,
        difficulty: null
      };
    }
  };
  